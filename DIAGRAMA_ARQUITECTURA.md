# 📊 DIAGRAMA DE ARQUITECTURA MULTI-TENANT

## Flujo de Datos Completo

```
┌─────────────────────────────────────────────────────────────────────────┐
│                            USUARIOS FINALES                              │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                    ┌───────────────┼───────────────┐
                    │               │               │
                    ▼               ▼               ▼
        ┌─────────────────┐ ┌─────────────┐ ┌─────────────────┐
        │   Tenant A      │ │  Tenant B   │ │  Super Admin    │
        │ eleganza.app.com│ │ chic.app.com│ │  admin.app.com  │
        └─────────────────┘ └─────────────┘ └─────────────────┘
                    │               │               │
                    └───────────────┼───────────────┘
                                    ▼
        ┌────────────────────────────────────────────────────────┐
        │           TENANT RESOLVER (tenant-resolver.js)         │
        │  - Detecta tenant desde URL (subdominio)               │
        │  - Carga configuración y branding del tenant           │
        │  - Inyecta contexto global (window.appContext)         │
        └────────────────────────────────────────────────────────┘
                                    │
                                    ▼
        ┌────────────────────────────────────────────────────────┐
        │           AUTH MANAGER (auth.js refactorizado)         │
        │  - Firebase Authentication                             │
        │  - Validación tenantId match                           │
        │  - Carga de permisos según rol                         │
        │  - Protección de rutas                                 │
        └────────────────────────────────────────────────────────┘
                                    │
                    ┌───────────────┼───────────────┐
                    │               │               │
                    ▼               ▼               ▼
        ┌─────────────────┐ ┌─────────────┐ ┌─────────────────┐
        │  Catálogo       │ │ Panel Admin │ │ Panel Super     │
        │  Público        │ │ (Tenant)    │ │ Admin           │
        │  (index.html)   │ │(admin.html) │ │(super-admin.html│
        └─────────────────┘ └─────────────┘ └─────────────────┘
                    │               │               │
                    └───────────────┼───────────────┘
                                    ▼
        ┌────────────────────────────────────────────────────────┐
        │              CAPA DE SERVICIOS (Modular)               │
        │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ │
        │  │Productos │ │  Ventas  │ │ Clientes │ │ Usuarios │ │
        │  │ Service  │ │ Service  │ │ Service  │ │ Service  │ │
        │  └──────────┘ └──────────┘ └──────────┘ └──────────┘ │
        │  - TODOS los queries incluyen filtro tenantId          │
        │  - Validación de permisos antes de operaciones         │
        └────────────────────────────────────────────────────────┘
                                    │
                                    ▼
        ┌────────────────────────────────────────────────────────┐
        │           FIREBASE FIRESTORE (Base de Datos)           │
        │                                                        │
        │  ┌──────────────────────────────────────────────────┐ │
        │  │ 🏢 tenants/                                      │ │
        │  │    ├── {tenantId}/                               │ │
        │  │    │   ├── nombre: "Boutique Eleganza"           │ │
        │  │    │   ├── slug: "eleganza"                      │ │
        │  │    │   ├── estado: "activo"                      │ │
        │  │    │   ├── branding: { logo, colores, textos }   │ │
        │  │    │   └── limites: { maxProductos, ... }        │ │
        │  └──────────────────────────────────────────────────┘ │
        │                                                        │
        │  ┌──────────────────────────────────────────────────┐ │
        │  │ 📦 productos/                                    │ │
        │  │    ├── {productoId}/                             │ │
        │  │    │   ├── tenantId: "tenant_abc123" ← CRÍTICO   │ │
        │  │    │   ├── nombre: "Vestido Rojo"                │ │
        │  │    │   ├── precio: 89000                         │ │
        │  │    │   └── imagenUrl: "/{tenantId}/productos..." │ │
        │  └──────────────────────────────────────────────────┘ │
        │                                                        │
        │  ┌──────────────────────────────────────────────────┐ │
        │  │ 💰 ventas/                                       │ │
        │  │    ├── {ventaId}/                                │ │
        │  │    │   ├── tenantId: "tenant_abc123" ← CRÍTICO   │ │
        │  │    │   ├── clienteNombre: "María García"         │ │
        │  │    │   └── total: 267000                         │ │
        │  └──────────────────────────────────────────────────┘ │
        │                                                        │
        │  ┌──────────────────────────────────────────────────┐ │
        │  │ 👤 usuarios/                                     │ │
        │  │    ├── {userId}/                                 │ │
        │  │    │   ├── tenantId: "tenant_abc123" | null      │ │
        │  │    │   ├── rol: "ADMIN_TENANT" | "SUPER_ADMIN"   │ │
        │  │    │   └── permisos: { productos_crear: true }   │ │
        │  └──────────────────────────────────────────────────┘ │
        │                                                        │
        │  Otras colecciones: clientes, pedidosWeb, apartados,  │
        │  categorias, etc. (TODAS con tenantId)                │
        └────────────────────────────────────────────────────────┘
                                    │
                                    ▼
        ┌────────────────────────────────────────────────────────┐
        │           FIREBASE SECURITY RULES                      │
        │  - Validación OBLIGATORIA de tenantId en writes        │
        │  - Usuarios solo acceden a datos de su tenant          │
        │  - Super Admins tienen acceso cross-tenant             │
        └────────────────────────────────────────────────────────┘
                                    │
                                    ▼
        ┌────────────────────────────────────────────────────────┐
        │           FIREBASE STORAGE                             │
        │  /{tenantId}/productos/{productoId}/imagen.jpg         │
        │  /{tenantId}/branding/logo.png                         │
        │  - Namespace estricto por tenant                       │
        └────────────────────────────────────────────────────────┘
```

---

## Jerarquía de Roles y Accesos

```
┌───────────────────────────────────────────────────────────────┐
│                     SUPER ADMIN                               │
│  - Gestiona TODA la plataforma                                │
│  - Crea/suspende/elimina tenants                              │
│  - Acceso cross-tenant (sin tenantId)                         │
│  - Panel: super-admin.html                                    │
└───────────────────────────────────────────────────────────────┘
                            │
                ┌───────────┴───────────┐
                │                       │
                ▼                       ▼
┌────────────────────────────┐ ┌────────────────────────────┐
│   ADMIN TENANT (Eleganza)  │ │   ADMIN TENANT (Chic)      │
│  - Gestiona SU empresa     │ │  - Gestiona SU empresa     │
│  - tenantId: "tenant_abc"  │ │  - tenantId: "tenant_xyz"  │
│  - Crea sub-usuarios       │ │  - Crea sub-usuarios       │
│  - Configura branding      │ │  - Configura branding      │
│  - Panel: admin.html       │ │  - Panel: admin.html       │
└────────────────────────────┘ └────────────────────────────┘
                │                       │
        ┌───────┴────────┐      ┌───────┴────────┐
        │                │      │                │
        ▼                ▼      ▼                ▼
┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│  VENDEDOR    │ │  INVENTARIO  │ │  CONTADOR    │ │  REPARTIDOR  │
│  (Eleganza)  │ │  (Eleganza)  │ │  (Chic)      │ │  (Chic)      │
│              │ │              │ │              │ │              │
│ - Ventas     │ │ - Productos  │ │ - Reportes   │ │ - Pedidos    │
│ - Apartados  │ │ - Stock      │ │ - Finanzas   │ │ - Entregas   │
│ - Clientes   │ │ - Categorías │ │ - Cierres    │ │              │
└──────────────┘ └──────────────┘ └──────────────┘ └──────────────┘
```

---

## Flujo de Identificación de Tenant

```
┌─────────────────────────────────────────────────────────────┐
│ 1. Usuario visita: https://eleganza.miboutique.com          │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 2. tenant-resolver.js se ejecuta al cargar                  │
│    - window.location.hostname = "eleganza.miboutique.com"   │
│    - Extrae subdomain: "eleganza"                           │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 3. Busca en Firestore:                                      │
│    db.collection('tenants')                                 │
│      .where('slug', '==', 'eleganza')                       │
│      .where('estado', '==', 'activo')                       │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 4. Si encontrado:                                           │
│    currentTenant = {                                        │
│      id: "tenant_abc123",                                   │
│      nombre: "Boutique Eleganza",                           │
│      branding: {                                            │
│        colorPrimario: "#8B4789",                            │
│        logo: "https://storage/.../logo.png",                │
│        textos: { nombreTienda: "Boutique Eleganza" }        │
│      }                                                       │
│    }                                                         │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 5. Inyectar branding dinámicamente:                         │
│    document.documentElement.style.setProperty(              │
│      '--color-primario', '#8B4789'                          │
│    )                                                         │
│    document.querySelector('[data-tenant-logo]').src = ...   │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 6. Guardar contexto global:                                 │
│    window.appContext = {                                    │
│      tenantId: "tenant_abc123",                             │
│      branding: {...}                                        │
│    }                                                         │
│    sessionStorage.setItem('currentTenant', JSON.stringify)  │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 7. TODOS los queries posteriores usan:                      │
│    .where('tenantId', '==', 'tenant_abc123')                │
└─────────────────────────────────────────────────────────────┘
```

---

## Flujo de Autenticación Multi-Tenant

```
┌─────────────────────────────────────────────────────────────┐
│ 1. Usuario en eleganza.miboutique.com/admin.html            │
│    (Contexto tenant ya cargado: tenant_abc123)              │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 2. Hace login con email/password                            │
│    Firebase Auth → userId: "user_xyz789"                    │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 3. auth-manager.js busca usuario:                           │
│    db.collection('usuarios').doc('user_xyz789').get()       │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 4. Valida pertenencia al tenant:                            │
│                                                              │
│    if (user.rol === "SUPER_ADMIN") {                        │
│      // Puede acceder a cualquier tenant                    │
│      permitirAcceso()                                        │
│    } else if (user.tenantId !== currentTenantId) {          │
│      // ERROR: Usuario no pertenece a este tenant           │
│      alert("No autorizado")                                 │
│      logout()                                                │
│    } else {                                                  │
│      // OK: Usuario pertenece al tenant actual              │
│      cargarPermisos()                                        │
│    }                                                         │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 5. Contexto completo:                                       │
│    window.appContext = {                                    │
│      tenantId: "tenant_abc123",                             │
│      userId: "user_xyz789",                                 │
│      rol: "ADMIN_TENANT",                                   │
│      permisos: {                                            │
│        productos_crear: true,                               │
│        productos_editar: true,                              │
│        ventas_crear: true,                                  │
│        usuarios_crear: true,                                │
│        ...                                                   │
│      }                                                       │
│    }                                                         │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 6. Cargar dashboard con datos filtrados:                    │
│    db.collection('productos')                               │
│      .where('tenantId', '==', 'tenant_abc123')              │
│      .get()                                                  │
└─────────────────────────────────────────────────────────────┘
```

---

## Security Rules en Acción

```
┌─────────────────────────────────────────────────────────────┐
│ Usuario intenta crear producto:                             │
│                                                              │
│ db.collection('productos').add({                            │
│   tenantId: "tenant_abc123",                                │
│   nombre: "Vestido Azul",                                   │
│   precio: 95000                                             │
│ })                                                           │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ Firestore Security Rules evalúan:                           │
│                                                              │
│ 1. ¿Usuario autenticado?                                    │
│    ✓ request.auth != null                                   │
│                                                              │
│ 2. ¿Tiene permiso "productos_crear"?                        │
│    ✓ get(usuarios/{userId}).data.permisos.productos_crear   │
│                                                              │
│ 3. ¿El tenantId match con el del usuario?                   │
│    ✓ request.resource.data.tenantId ==                      │
│      get(usuarios/{userId}).data.tenantId                   │
│                                                              │
│ 4. SI todo es TRUE → PERMITIR                               │
│    SINO → RECHAZAR con error "Permission denied"            │
└─────────────────────────────────────────────────────────────┘
```

**Caso de ataque**: Usuario de Tenant A intenta modificar tenantId a Tenant B

```javascript
// ❌ Intento malicioso
db.collection('productos').doc('prod_123').update({
  tenantId: "tenant_xyz999" // Intentando cambiar a otro tenant
})

// 🛡️ Security Rules RECHAZAN:
// - Usuario pertenece a tenant_abc123
// - Intenta modificar tenantId (campo protegido)
// - Rule: No se permite cambiar tenantId una vez creado
// - RESULTADO: Permission denied
```

---

## Branding Dinámico en Acción

```
┌─────────────────────────────────────────────────────────────┐
│ Tenant: Boutique Eleganza                                   │
├─────────────────────────────────────────────────────────────┤
│ Configuración:                                              │
│  - colorPrimario: #8B4789 (Morado)                          │
│  - logo: eleganza-logo.png                                  │
│  - nombreTienda: "Boutique Eleganza"                        │
└─────────────────────────────────────────────────────────────┘
                            ↓
        ┌───────────────────────────────────┐
        │    CSS Variables Inyectadas       │
        ├───────────────────────────────────┤
        │ --color-primario: #8B4789         │
        │ --color-secundario: #333333       │
        │ --color-acento: #FFD700           │
        └───────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    index.html renderiza:                     │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  [Logo: eleganza-logo.png]  BOUTIQUE ELEGANZA         │  │
│  ├───────────────────────────────────────────────────────┤  │
│  │  [Botón Morado]  Ver Catálogo                         │  │
│  │  [Card con borde morado] Vestido Rojo - $89,000      │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘

VS

┌─────────────────────────────────────────────────────────────┐
│ Tenant: Chic Store                                          │
├─────────────────────────────────────────────────────────────┤
│ Configuración:                                              │
│  - colorPrimario: #E91E63 (Rosa)                            │
│  - logo: chic-logo.png                                      │
│  - nombreTienda: "Chic Store"                               │
└─────────────────────────────────────────────────────────────┘
                            ↓
        ┌───────────────────────────────────┐
        │    CSS Variables Inyectadas       │
        ├───────────────────────────────────┤
        │ --color-primario: #E91E63         │
        │ --color-secundario: #212121       │
        │ --color-acento: #00BCD4           │
        └───────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    index.html renderiza:                     │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  [Logo: chic-logo.png]  CHIC STORE                    │  │
│  ├───────────────────────────────────────────────────────┤  │
│  │  [Botón Rosa]  Ver Catálogo                           │  │
│  │  [Card con borde rosa] Blusa Elegante - $65,000      │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

**MISMO CÓDIGO HTML/CSS, DIFERENTE APARIENCIA**

---

## Estructura de Archivos Post-Refactorización

```
mi-boutique/
│
├── src/
│   ├── core/                              # Módulos centrales
│   │   ├── tenant-resolver.js             # ★ Detecta tenant desde URL
│   │   ├── auth-manager.js                # ★ Auth multi-tenant
│   │   ├── permissions.js                 # Sistema RBAC
│   │   └── firebase-config.js             # Config Firebase
│   │
│   ├── services/                          # Capa de datos
│   │   ├── productos.service.js           # CRUD productos (filtrado)
│   │   ├── ventas.service.js              # CRUD ventas (filtrado)
│   │   ├── clientes.service.js            # CRUD clientes (filtrado)
│   │   ├── usuarios.service.js            # CRUD usuarios (filtrado)
│   │   ├── tenants.service.js             # ★ CRUD tenants (super admin)
│   │   └── planes.service.js              # ★ Gestión de planes
│   │
│   ├── ui/                                # Componentes UI
│   │   ├── navbar.component.js            # Navbar con branding dinámico
│   │   ├── sidebar.component.js           # Sidebar del admin
│   │   ├── modal.component.js             # Modales reutilizables
│   │   ├── theme-injector.js              # ★ Inyección de CSS variables
│   │   └── table.component.js             # Tablas reutilizables
│   │
│   ├── pages/
│   │   ├── public/
│   │   │   ├── index.html                 # Catálogo público
│   │   │   └── index.js                   # app.js refactorizado
│   │   │
│   │   ├── admin/
│   │   │   ├── admin.html                 # Panel admin tenant
│   │   │   ├── admin.js                   # admin.js refactorizado
│   │   │   ├── productos.controller.js    # Controlador productos
│   │   │   ├── ventas.controller.js       # Controlador ventas
│   │   │   └── branding.controller.js     # ★ Configuración branding
│   │   │
│   │   └── super-admin/
│   │       ├── super-admin.html           # ★ Panel super admin
│   │       ├── super-admin.js             # ★ Lógica super admin
│   │       ├── tenants.controller.js      # ★ Gestión tenants
│   │       └── platform-metrics.js        # ★ Métricas globales
│   │
│   └── styles/
│       ├── theme.css                      # ★ Variables CSS base
│       ├── components.css                 # Estilos de componentes
│       ├── admin.css                      # Estilos admin panel
│       └── super-admin.css                # ★ Estilos super admin
│
├── functions/                             # ★ Cloud Functions (opcional)
│   ├── index.js                           # Punto de entrada
│   ├── tenant-setup.js                    # Onboarding automatizado
│   ├── backup.js                          # Backup por tenant
│   └── analytics.js                       # Métricas agregadas
│
├── firestore.rules                        # ★ Security Rules actualizadas
├── storage.rules                          # ★ Storage Rules actualizadas
├── firebase.json                          # ★ Config hosting + subdominios
├── package.json                           # Dependencias
├── PROPUESTA_SAAS_MULTITENANT.md          # Este documento
└── DIAGRAMA_ARQUITECTURA.md               # Diagramas visuales
```

**Leyenda**: ★ = Archivo nuevo o requiere modificación profunda

---

## Índices Firestore Requeridos

```
productos_tenantId_timestamp (Compuesto)
├── tenantId (ASC)
└── timestamp (DESC)

productos_tenantId_categoriaId (Compuesto)
├── tenantId (ASC)
└── categoriaId (ASC)

productos_tenantId_visible (Compuesto)
├── tenantId (ASC)
└── visible (ASC)

ventas_tenantId_timestamp (Compuesto)
├── tenantId (ASC)
└── timestamp (DESC)

ventas_tenantId_estado (Compuesto)
├── tenantId (ASC)
└── estado (ASC)

clientes_tenantId_nombre (Compuesto)
├── tenantId (ASC)
└── nombreLower (ASC)

usuarios_tenantId_rol (Compuesto)
├── tenantId (ASC)
└── rol (ASC)

tenants_slug (Simple)
└── slug (ASC)

tenants_estado (Simple)
└── estado (ASC)
```

Estos índices se crearán automáticamente cuando Firestore los requiera, o se pueden crear manualmente desde la consola de Firebase.

---

## Conclusión

Esta arquitectura multi-tenant proporciona:

✅ **Aislamiento total de datos** (Security Rules + tenantId)
✅ **Escalabilidad horizontal** (Firebase auto-scale)
✅ **White-label completo** (CSS variables dinámicas)
✅ **RBAC robusto** (3 niveles de permisos)
✅ **Costo-efectivo** (Base de datos compartida)
✅ **Mantenimiento centralizado** (Una base de código)
✅ **Seguridad enterprise** (Validación server-side)

¿Todo claro? ¿Procedemos con la implementación? 🚀
